ИНСТРУКЦИЯ ПО РУЧНОМУ ИСПРАВЛЕНИЮ ОШИБКИ:

Откройте файл src/server/index.tsx 

Найдите строку 2678, которая выглядит так:
             }

После этой строки вставьте новую строку с закрывающей скобкой:
            }

Должно получиться:
             }
            }
         } catch (e) {

Это исправит ошибку "Expected a semicolon" на строке 2679.

Проблема: На строке 2627 открывается блок `if (!error && wonDeals && wonDeals.length > 0) {`
но нет закрывающей скобки перед строкой 2679 `} catch (e) {`
